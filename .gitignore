# Never commit ROMs or copyrighted assets
baserom.gba
*.gba
*.elf
*.map

# Build artifacts
build/

# Python
__pycache__/
*.pyc

# OS
.DS_Store
pending/
# generated by tools/gen_report.py / CI
report.json
# local Pi transcripts and autopilot bookkeeping; the harness resources
# themselves are versioned.  A trailing comment on a .gitignore line is
# part of the pattern, not a comment, so these must stay on their own line.
.pi/sessions/
.pi/state/
